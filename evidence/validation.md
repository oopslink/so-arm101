# 文档与脚本验收（2026-09-12）

本次在Mac完成的检查：

- 所有Python脚本通过AST语法检查。
- POSIX采集脚本通过`sh -n`；云依赖脚本通过`bash -n`。
- `python3 scripts/test_docs_tools.py`：4项测试通过（语法、精确/缩写step、续训顺序、拒绝空postprocessor）。
- 新指标导出脚本读取历史完整V3日志，得到3000条记录，不依赖W&B。
- 在项目独立`.venv-docs`环境安装matplotlib/numpy，实际执行绘图脚本，人工检查输出。
- 两张SVG流程/数学图渲染为PNG并人工检查；三张历史截图和V3训练图均人工查看。
- Markdown本地链接目标全部存在；文本凭据模式扫描未发现明文HF/W&B token或私钥。
- 新脚本帮助命令正常；配置示例可由POSIX shell加载。

未执行：真实采集、云端安装/训练、全量数据合并、完整π0权重/processor集成加载、机械臂动作。只读核验始终遵守“不启动机器人实测”的边界；本地单元测试不能替代这些集成测试。

机器人工作站的只读检查见[spark-readonly-audit.md](spark-readonly-audit.md)：读取软件版本、脚本哈希、批编码补丁、模型配置与tokenizer路径；遍历V3全部50,596行验证future4与全局统计；解析历史60秒动作记录并绘图查看。没有启动摄像头或机器人连接代码，也没有重新加载或运行策略。

新打包/验证脚本是防回归工具，不是已在所有LeRobot版本上验证的一键部署器。历史模型工件没有被本次修改，HF也没有被本次更新。
