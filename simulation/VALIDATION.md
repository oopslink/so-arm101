# 本地验证记录

日期：2026-09-13。平台：macOS ARM64，Python 3.12.13。
依赖组合见 `requirements-macos-arm64.lock.txt`，原始汇总见 [validation-results.json](validation-results.json)。

## 已完成

| 检查 | 结果 |
|---|---|
| SB3 环境接口检查 | 通过，32 维观测 / 5 维动作 |
| 自动测试 | 10 项通过 |
| 脚本敲击 | 种子 10000–10019，20/20 成功 |
| 脚本录像 | 成功输出 MP4；检查准备、接触、抬起三帧 |
| PPO 双进程短训练 | 4,096 步完成，生成日志、checkpoint、最终模型 |
| checkpoint 恢复 | 从最终模型额外训练 512 步完成 |
| 独立 PPO 评估 | 种子 20000–20004，0/5 成功；均记录一次有效接触，未完成抬起，最终超时 |

脚本控制器的 100% 成功率只证明小范围初始条件下环境可解；它不代表学习策略的成功率。
PPO 这次仅为训练流程验证，未完成收敛训练。

测试包括：有效接触、区域与速度过滤、持续接触不重复计数、二次撞击、分离成功、确定性复位、超时、非法动作、桌面碰撞，以及脚本经过物理执行器完成敲击。

首次桌面碰撞测试修改静态几何后未刷新 MuJoCo 常量，未触发预期碰撞；测试改为刷新常量并恢复姿态后通过。不是忽略碰撞来通过测试。

## 本机产物（Git 忽略）

- `runs/scripted.mp4`：三次独立脚本回合录像。
- `runs/preview.png`：准备、接触、抬起对照图。
- `runs/scripted-20.json`：20 次脚本评估。
- `runs/baseline-smoke/`：4,096 步训练产物。
- `runs/baseline-resume/`：恢复后的模型和配置。
- `runs/ppo-evaluation.json`：五次独立模型评估。

上述路径相对 `simulation/`。模型观测接口为 32 维；开发中更早的临时目录 `smoke-ppo/` 使用了 31 维接口，不属于当前交付模型。

## 尚未验证

- Spark ARM64 安装、EGL 录像及训练吞吐。
- 多随机种子的收敛训练与更大位置扰动。
- 木鱼及槌的实物尺寸、动力学和电机标定一致性。
- 真实抓握、视觉输入、三次敲击、放回与仿真到真机迁移。

## 虎头场景接入后的验证

默认环境现为实测尺寸的虎头与底板，固定持白头槌；历史数据不覆盖。

- 12 项测试通过，包含新场景尺寸、无重复木槌、旧配置保持圆柱环境、碰撞失败和脚本敲击。
- 种子 10000–10019：虎头场景脚本敲击 20/20 成功，记录见 `tiger-validation-results.json`。
- 已重新生成 `runs/scripted.mp4`，并检查视频帧。
- `mjpython -m wooden_fish.play --episodes 1` 成功创建实时窗口、完成一回合、自动关闭。
- 新场景 512 步 PPO 流程测试完成，仅证明接口可训练，未评估收敛。
- 底板相对机械臂的位置和具体敲击区域仍为暂定；单次敲击从已持槌状态开始，未包含初始摆放到抓取。
